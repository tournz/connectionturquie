<div class="container">
  <%= simple_form_for @booking do |f| %>
  <%= f.input :date, label: 'Date', as: :string, input_html: {class: "datepicker"} %>
  <%= f.input :start_time, label: 'Heure de début', as: :time, minute_step: 15 %>
  <%= f.input :end_time, label: 'Heure de fin', as: :time, minute_step: 15 %>
  <%= f.submit "Book Zacharie", class: "btn btn-primary" %>
  <% end %>
</div>
